[Unit]
Description={{summary}}
Requires=network.target
After=network.target

[Service]
ExecStart={{service.target}}
PIDFile=/var/run/{{service.name}}.pid
Environment="NODE_ENV=production"

{{#if service.user}}
User={{service.user}}
{{/if}}
{{#if service.group}}
Group={{service.group}}
{{/if}}

[Install]
WantedBy=multi-user.target
